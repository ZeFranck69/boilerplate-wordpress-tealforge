stages:
  - quality

quality:
  stage: quality
  image: node:22-bookworm
  before_script:
    - apt-get update
    - apt-get install --yes --no-install-recommends php-cli
  script:
    - bin/ci-check

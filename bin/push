#!/usr/bin/env bash
set -euo pipefail

REMOTE="${REMOTE:-origin}"
BRANCH="${BRANCH:-$(git branch --show-current)}"

if [ -z "$BRANCH" ]; then
  echo "Branche Git introuvable."
  exit 1
fi

bin/check
git push "$REMOTE" "$BRANCH"
